{
  "name": "picture-descriptor",
  "version": "0.2.0",
  "description": "Describes images using AI via sub-agent sessions. Tiers: local (llamaswap/gemma4), remote-free (github-copilot/gpt-5-mini), remote-cheap (opencode-go/mimo-v2.5), remote-ux (opencode-go/kimi-k2.5), remote-general (opencode-go/qwen3.6-plus). Formats: natural or structured JSON.",
  "type": "module",
  "keywords": [
    "pi-package",
    "pi-extension",
    "vision",
    "image-description"
  ],
  "scripts": {
    "validate": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "peerDependencies": {
    "@earendil-works/pi-ai": "*",
    "@earendil-works/pi-coding-agent": "*"
  },
  "dependencies": {
    "sharp": "0.34.5",
    "typebox": "*"
  },
  "devDependencies": {
    "@earendil-works/pi-coding-agent": "*",
    "typescript": "^5.3.0",
    "vitest": "^4.1.6"
  },
  "pi": {
    "extensions": [
      "./src/picture-descriptor.ts"
    ]
  }
}
